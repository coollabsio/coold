name: Nightly Release

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: nightly-${{ github.sha }}
  cancel-in-progress: false

permissions:
  contents: write
  packages: write

jobs:
  prep:
    runs-on: ubuntu-24.04
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
      - name: Compute tag
        id: meta
        run: echo "tag=nightly-$(date -u +%Y%m%d)-$(git rev-parse --short=8 HEAD)" >> "$GITHUB_OUTPUT"

  build:
    needs: prep
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
            arch: amd64
          - runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            arch: arm64
    runs-on: ${{ matrix.runner }}
    env:
      COOLD_VERSION: ${{ needs.prep.outputs.tag }}
      SCHEDULER_VERSION: ${{ needs.prep.outputs.tag }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}
          cache-all-crates: true
          cache-on-failure: true
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: Install protoc
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler

      - name: Build workspace
        run: cargo build --workspace --release --locked --target ${{ matrix.target }}

      - name: Package coold
        run: |
          mkdir -p dist
          cp target/${{ matrix.target }}/release/coold dist/coold
          tar -czf dist/coold-linux-${{ matrix.arch }}.tar.gz -C dist coold
          rm dist/coold
          (cd dist && sha256sum coold-linux-${{ matrix.arch }}.tar.gz > coold-linux-${{ matrix.arch }}.tar.gz.sha256)

      - name: Package scheduler
        run: |
          cp target/${{ matrix.target }}/release/scheduler dist/scheduler
          tar -czf dist/scheduler-linux-${{ matrix.arch }}.tar.gz -C dist scheduler
          rm dist/scheduler
          (cd dist && sha256sum scheduler-linux-${{ matrix.arch }}.tar.gz > scheduler-linux-${{ matrix.arch }}.tar.gz.sha256)

      - name: Package builder
        run: |
          cp target/${{ matrix.target }}/release/builder dist/builder
          tar -czf dist/builder-linux-${{ matrix.arch }}.tar.gz -C dist builder
          rm dist/builder
          (cd dist && sha256sum builder-linux-${{ matrix.arch }}.tar.gz > builder-linux-${{ matrix.arch }}.tar.gz.sha256)

      - uses: actions/upload-artifact@v4
        with:
          name: coold-linux-${{ matrix.arch }}
          path: dist/*
          retention-days: 7

  scheduler-image:
    needs: prep
    runs-on: ubuntu-24.04
    env:
      TAG: ${{ needs.prep.outputs.tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Compute image name
        run: echo "IMAGE_NAME=ghcr.io/${GITHUB_REPOSITORY,,}/scheduler" >> "$GITHUB_ENV"

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=nightly
            type=raw,value=${{ env.TAG }}
          labels: |
            org.opencontainers.image.title=Coolify scheduler
            org.opencontainers.image.description=Central-side gRPC stream scheduler for coold agents.

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: scheduler/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            SCHEDULER_VERSION=${{ env.TAG }}
          cache-from: type=gha,scope=scheduler-image
          cache-to: type=gha,scope=scheduler-image,mode=max

  release:
    needs: [prep, build, scheduler-image]
    runs-on: ubuntu-24.04
    env:
      TAG: ${{ needs.prep.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Compose release notes
        id: notes
        run: |
          PREV=$(git tag --list 'nightly-*' --sort=-creatordate | head -n1)
          {
            echo "body<<EOF"
            echo "Automated nightly build from \`${GITHUB_SHA}\`."
            echo
            echo "**Version:** \`${TAG}\`"
            echo "**Commit:** $(git log -1 --pretty=format:'%s')"
            echo
            echo "### Artifacts"
            echo "- \`coold\` — per-host agent (holds the single gRPC stream to scheduler; spawns builder subprocess per BuildRequest when COOLD_BUILDER_ENABLED=1)"
            echo "- \`scheduler\` — central-side gRPC stream scheduler (single listener on :6443; capability-aware routing)"
            echo "- \`ghcr.io/${GITHUB_REPOSITORY,,}/scheduler:${TAG}\` — multi-arch scheduler OCI image"
            echo "- \`builder\` — short-lived build subprocess invoked by coold under a \`systemd-run --scope\` transient unit"
            echo
            if [ -n "$PREV" ]; then
              echo "### Changes since $PREV"
              git log --pretty=format:'- %s (%h)' "$PREV"..HEAD
              echo
            fi
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Create per-commit release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG }}
          name: Nightly ${{ env.TAG }}
          body: ${{ steps.notes.outputs.body }}
          prerelease: true
          make_latest: false
          files: |
            dist/coold-linux-amd64.tar.gz
            dist/coold-linux-amd64.tar.gz.sha256
            dist/coold-linux-arm64.tar.gz
            dist/coold-linux-arm64.tar.gz.sha256
            dist/scheduler-linux-amd64.tar.gz
            dist/scheduler-linux-amd64.tar.gz.sha256
            dist/scheduler-linux-arm64.tar.gz
            dist/scheduler-linux-arm64.tar.gz.sha256
            dist/builder-linux-amd64.tar.gz
            dist/builder-linux-amd64.tar.gz.sha256
            dist/builder-linux-arm64.tar.gz
            dist/builder-linux-arm64.tar.gz.sha256

      - name: Refresh rolling nightly release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete nightly --yes --cleanup-tag 2>/dev/null || true
          gh release create nightly \
            --prerelease \
            --title "Nightly (latest)" \
            --target "${GITHUB_SHA}" \
            --notes "Latest nightly build — mirrors [${TAG}](../../releases/tag/${TAG})." \
            dist/coold-linux-amd64.tar.gz \
            dist/coold-linux-amd64.tar.gz.sha256 \
            dist/coold-linux-arm64.tar.gz \
            dist/coold-linux-arm64.tar.gz.sha256 \
            dist/scheduler-linux-amd64.tar.gz \
            dist/scheduler-linux-amd64.tar.gz.sha256 \
            dist/scheduler-linux-arm64.tar.gz \
            dist/scheduler-linux-arm64.tar.gz.sha256 \
            dist/builder-linux-amd64.tar.gz \
            dist/builder-linux-amd64.tar.gz.sha256 \
            dist/builder-linux-arm64.tar.gz \
            dist/builder-linux-arm64.tar.gz.sha256 \
